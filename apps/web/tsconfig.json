{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "preserve",
    "allowJs": true,
    "noEmit": true,
    // apps/api ships raw TS and imports with explicit .ts specifiers
    // (it runs on Node 24 type-stripping, no build step).
    "allowImportingTsExtensions": true,
    "declaration": false,
    "incremental": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "types": ["node"],
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
